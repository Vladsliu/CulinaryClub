@model IEnumerable<MasterClass>


<div class="album py-5 bg-light">
    <div class="container">
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
            @*@foreach(var item in Model)*@
            @*@for (int n = 0; 5>n; n++)*@
            {
            <div class="col">
                <div class="card shadow-sm">
                    <svg class="bd-placeholder-img card-img-top" width="100%" height="225" ">Cart image</text></svg>@*//5 tedi 18-20*@
                    <div class="card-body">
                        @*  <p class="card-text">@item.Title</p>
                        <p class="card-text">@item.Description</p>*@
                         <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                        
                        <a asp-controller="Club" asp-action="Detail" @*asp-route-id="@item.Id"*@ class="btn btn-sm btn-outline-secondary">View</a>
                        <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                        </div>
                        <small class="text-muted">@item.Category</small>
                        </div>
                    </div>
                </div>
            </div>
            }
        </div>
    </div>
</div>